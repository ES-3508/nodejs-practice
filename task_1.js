// Task 1: Array Manipulation
function sumEvenNumbers(arr) {
  // Your code here
}

// Example usage:
const numbers = [1, 2, 3, 4, 5, 6];
console.log(sumEvenNumbers(numbers)); // Output: 12 (2 + 4 + 6)

// Hint: 
// 1. Use filter to get even numbers: arr.filter(num => num % 2 === 0)
// 2. Use reduce to sum the even numbers: arr.reduce((acc, num) => acc + num, 0)
