function doubleNumbers(arr) {
  // Your code here
}

// Example usage:
const numbers = [1, 2, 3, 4, 5];
console.log(doubleNumbers(numbers)); // Output: [2, 4, 6, 8, 10]

// Hint:
// Use map to double each number: arr.map(num => num * 2)
